name: Repo Contract

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  repo-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Contract
        run: .agentic-template/bin/project repo-check
